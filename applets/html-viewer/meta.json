{
  "slug": "html-viewer",
  "name": "HTML Viewer",
  "description": "Render an HTML file in a sandboxed iframe (interactive but isolated)",
  "params": {
    "path": {
      "required": true,
      "description": "Absolute path to HTML file"
    }
  },
  "createdAt": "2026-05-09T00:00:00.000Z",
  "updatedAt": "2026-05-09T00:00:00.000Z",
  "agentUsage": {
    "purpose": "Display a rendered HTML artifact (report, explainer, interactive visualization). The HTML runs in a sandboxed iframe — scripts and styles work but cannot access Caco's session, storage, or DOM."
  },
  "stateSchema": {
    "get": {
      "path": "string",
      "loaded": "boolean"
    },
    "set": null
  }
}
